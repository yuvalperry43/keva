Review the git diff, then commit and push.

Steps:
1. Run `git diff HEAD` and `git diff --cached` to see all changes.
2. Scan the diff for any new actionable tasks relevant to an Israeli career soldier (e.g. new financial tips, benefits, steps to take) that aren't already in checklist.html. If you find any candidates, list them and ask the user whether to add them to the checklist before committing. Wait for the user's response before proceeding.
3. Once any checklist updates are done (or if there were none), proceed with the standard commit flow: stage relevant files, write a commit message, commit, then push to remote.
